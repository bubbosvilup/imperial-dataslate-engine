# Imperial Dataslate Engine

A modular reminder engine for Warhammer 40,000.  
Faction rules are stored as JSON files and processed through a lightweight TypeScript backend.  
A separate React UI presents reminders in phase order.

Inspired by the **AoS Reminders** project.

